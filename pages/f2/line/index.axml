<view class="page" style="padding:0">
    <view class="page-section">
    <view class="page-section-title">折线图</view>
      <line
        categories="{{categories}}"
        series="{{series}}"
        xAxis="{{xAxis}}"
        yAxis="{{yAxis}}"
        legend="{{legend}}"
        tooltip="{{tooltip}}"
      />
  </view>
</view>